// export const userRegEx = ``;

// regex.ts

export const numbersRegExp: RegExp = /^[0-9]+$/;
export const userRegExp: RegExp = /^[\w.]+$/;
export const alphanumericRegExp: RegExp = /^[a-zA-Z0-9]+$/;
export const emailRegExp: RegExp = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const ipAddressRegExp: RegExp = /^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$/;